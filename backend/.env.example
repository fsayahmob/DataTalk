# =============================================================================
# G7 Analytics - Configuration Environnement
# =============================================================================
# Copier ce fichier en .env et remplir les valeurs

# -----------------------------------------------------------------------------
# OBLIGATOIRE EN PRODUCTION
# -----------------------------------------------------------------------------

# Clé de chiffrement pour les secrets (API keys stockées en DB)
# Générer avec: python -c "import os, base64; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
ENCRYPTION_KEY=

# Environnement (development | production)
# En production, ENCRYPTION_KEY est obligatoire
ENVIRONMENT=development

# -----------------------------------------------------------------------------
# OPTIONNEL - Base de données
# -----------------------------------------------------------------------------

# Chemin vers la base DuckDB (défaut: ../data/g7_analytics.duckdb)
# DUCKDB_PATH=../data/g7_analytics.duckdb

# -----------------------------------------------------------------------------
# OPTIONNEL - LLM
# -----------------------------------------------------------------------------

# Clé API Gemini (peut aussi être configurée via l'interface)
# GEMINI_API_KEY=

# Modèle par défaut
# DEFAULT_MODEL=gemini/gemini-2.0-flash
